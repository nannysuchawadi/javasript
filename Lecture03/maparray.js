const finalParticipants = ["Taylor", "Donald", "Don", "Natasha", "Bobby"];

const annnouncements = finalParticipants.map((menubar) => {
    return menubar + "joined the contest.";
});

console.log(annnouncements);